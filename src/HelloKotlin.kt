fun main() {

    val colors = ArrayList<String>()
    colors.add("wight")
    colors.add("red")
    colors.add("back")
    colors.add("Green")

    for (n in 0 until colors.size ){
        println(colors[n])
    }
    println("----------------")
    for (color in colors){
        println(color)
    }
}